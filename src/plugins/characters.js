const Plugin = require('../structures/plugin');

module.exports = new Plugin({
    name: 'Characters',
    embeded: '::bust_in_silhouette: Anime Characters',
    visible: true,
    enabled: true,
    commands: [
        {
            command: ''
        }
    ]
});